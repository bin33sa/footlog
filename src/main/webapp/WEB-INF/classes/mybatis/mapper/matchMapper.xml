<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fl.mapper.MemberMapper">
	
	<insert id="insertMatch">
		INSERT INTO match_post(match_code,home_code,away_code,match_date,status,title,content,created_at,view_count)
		VALUES(seq_match_post.NEXTVAL, #{home_code},NULL,TO_CHAR(match_date,'YYYY-MM-DD')match_date, 
		'모집중',#{title},#{content},SYSDATE,0)
		
	</insert>
	
	<!-- 팀코드 가져오기 -->
	<select id="getTeamcodeById" parameterType="Long" resultType="com.fl.model.MatchDTO">
		SELECT t.team_code 
		FROM team t
		JOIN member_team mt ON t.team_code = mt.team_code
	</select>
	
	<!-- 글 리스트 -->
	<select id="listMatch" parameterType="map" resultType="com.fl.model.MatchDTO">
		SELECT t.team_code,team_name, match_date, title,content,view_count, 
		FROM team t
		JOIN member_team mt ON t.team_code = mt.team_code
		WHERE mt.member_code = #{member_code}
	</select>
</mapper>