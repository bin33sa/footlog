<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fl.mapper.AdminMypageMapper">

	
		<!-- 구단 전체 수 조회 -->
		<select id="CountTeamAll"
			resultType="com.fl.model.TeamDTO">
			SELECT
			*
			FROM team
		</select>
		
		<!-- 구장 전체 수 조회 -->
		<select id="CountStadiumAll"
			resultType="com.fl.model.StadiumDTO">

			SELECT
			*
			FROM stadium
		</select>
		
		<!-- 멤버 전체 수 조회 -->
		<select id="CountMemberAll"
			resultType="com.fl.model.MemberDTO">

			SELECT
			*
			FROM member
		</select>


</mapper>